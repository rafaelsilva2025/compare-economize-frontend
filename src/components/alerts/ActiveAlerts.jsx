import React from "react";

/**
 * ActiveAlerts (sem Base44)
 *
 * Esta funcionalidade dependia do Base44 (auth + entities).
 * Por enquanto, desativamos para manter o app 100% próprio.
 *
 * Depois podemos reimplementar com sua API:
 * - GET /api/price-alerts?productId=...
 * - POST /api/price-alerts
 * - DELETE /api/price-alerts/:id
 */
export default function ActiveAlerts() {
  return null;
}
